package math;

public class MathSinifi {
  /*
            Math.abs(x)      // Mutlak değer
            Math.max(a,b)    // Büyük olanı al
            Math.min(a,b)    // Küçük olanı al
            Math.pow(a,b)    // Üs alma
            Math.sqrt(x)     // Kareköp
            Math.round(x)    // Yakın tam sayıya yuvarla
            Math.floor(x)    // Aşağı yuvarla
            Math.ceil(x)     // Yukarı yuvarla
            Math.random()    // 0-1 arası rastgele sayı
            Math.sin(x)      // Sinüs
            Math.cos(x)      // Kosinüs
            Math.tan(x)      // Tanjant
            Math.toDegrees(x)// Radyanı dereceye çevir
            Math.toRadians(x)// Dereceyi radyana çevir
*/
    public static void main(String[] args){
        System.out.println(Math.sin(30));
        System.out.println(Math.ceil(2.1));
        System.out.println(Math.random());
        System.out.println(Math.pow(4,2));
        System.out.println(Math.sqrt(4));
        System.out.println(Math.abs(-15));
    }
}
